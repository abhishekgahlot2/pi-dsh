# `npm start` loads this file automatically after you copy it to `.env`.
PIDSH_MODEL=<openrouter-model-id>

# Optional: pi-dsh also reads `openrouter_key=...` from ~/ai_keys_loop.
# PIDSH_OPENROUTER_KEY_NAME=openrouter_key
# OPENROUTER_API_KEY=
